{
    "debug": false,
    "database": "root:123456@tcp(192.168.228.129:3306)/falcon_portal?loc=Local&parseTime=true", //Portal的数据库地址
    "hosts": "", //portal数据库中有个host表，如果表中数据是从其他系统同步过来的，此处配置为sync，否则就维持默认，留空即可
    "maxConns": 20,
    "maxIdle": 15,
    "listen": ":6030", //hbs监听的rpc地址
    "trustable": [""],
    "http": {
        "enabled": true,
        "listen": "0.0.0.0:6031" //hbs监听的http地址
    }
}
